# نحوه اجرای AYMusic Player

## روش اول: استفاده از AppImage (توصیه می‌شود برای تست سریع)

```bash
# مجوز اجرا دادن به فایل
chmod +x "AYMusic Player-1.0.0.AppImage"

# اجرای برنامه
./"AYMusic Player-1.0.0.AppImage"
```

## روش دوم: نصب با .deb (برای نصب دائمی در سیستم)

```bash
# نصب
sudo dpkg -i aymusic-player_1.0.0_amd64.deb

# اجرا از منو یا با دستور
aymusic-player
```

## امکانات برنامه:

✅ پخش فایل‌های صوتی (MP3, WAV, OGG, FLAC, M4A, AAC, WMA)
✅ دکمه‌های کنترل: Play, Pause, Stop, Previous, Next
✅ مدیریت لیست پخش
✅ تغییر تم (روشن/تاریک/سیستم)
✅ کنترل حجم صدا
✅ نمایش زمان پخش
✅ طراحی مینیمال و شیک

## توضیحات تکمیلی:

- فایل‌های ساخته شده در پوشه `dist/` قرار دارند
- AppImage نیاز به نصب ندارد و portable است
- فایل .deb را می‌توان در اوبونتو و توزیع‌های مبتنی بر دبیان نصب کرد
- برنامه با ElectronJS و React ساخته شده است

## مشکلات احتمالی:

اگر در اجرای مستقیم با `npm run dev` با خطا مواجه شدید:

- این مشکل مربوط به GTK و GPU در Electron است
- اما فایل‌های build شده (.deb و AppImage) بدون مشکل کار می‌کنند
- می‌توانید با فلگ‌های `--no-sandbox --disable-gpu` اجرا کنید
